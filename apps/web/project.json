{
  "name": "web",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/web/src",
  "targets": {
    "dev": {
      "command": "panda codegen --clean && panda cssgen && vite",
      "options": {
        "cwd": "apps/web"
      }
    },
    "build": {
      "command": "panda codegen --clean && panda cssgen && vite build",
      "options": {
        "cwd": "apps/web"
      },
      "outputs": ["{projectRoot}/dist"]
    },
    "serve": {
      "command": "vite preview",
      "options": {
        "cwd": "apps/web"
      }
    }
  }
}
