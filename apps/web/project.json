{
	"name": "web",
	"$schema": "../../node_modules/nx/schemas/project-schema.json",
	"projectType": "application",
	"sourceRoot": "apps/web/src",
	"targets": {
		"dev": {
			"command": "pnpm gen:custom-runner && panda codegen --clean && panda cssgen && vite",
			"options": {
				"cwd": "apps/web"
			}
		},
		"build": {
			"command": "pnpm gen:custom-runner && panda codegen --clean && panda cssgen && vite build",
			"options": {
				"cwd": "apps/web"
			},
			"outputs": ["{projectRoot}/dist"]
		},
		"serve": {
			"command": "vite preview",
			"options": {
				"cwd": "apps/web"
			}
		}
	}
}
