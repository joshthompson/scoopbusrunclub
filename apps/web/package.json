{
  "name": "@scoopbus/web",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "panda codegen --clean && panda cssgen && vite",
    "dev": "panda codegen --clean && panda cssgen && vite",
    "build": "panda codegen --clean && panda cssgen && vite build",
    "serve": "vite preview"
  },
  "dependencies": {
    "@solidjs/router": "^0.15.4",
    "qr-scanner": "^1.4.2"
  }
}
