{
  "name": "api",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "application",
  "sourceRoot": "apps/api/convex",
  "targets": {
    "dev": {
      "command": "npx convex dev",
      "options": {
        "cwd": "apps/api"
      }
    },
    "deploy": {
      "command": "npx convex deploy",
      "options": {
        "cwd": "apps/api"
      }
    }
  }
}
